{
  "name": "civic-service-system",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @civic/web",
    "dev:api": "npm run start:dev --workspace @civic/api",
    "build": "npm run build --workspace @civic/types && npm run build --workspace @civic/ui && npm run build --workspace @civic/api && npm run build --workspace @civic/web",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspace @civic/api"
  }
}

